# Drug-timing arm — FEASIBILITY / PLUMBING demo (NOT a validation)
> Generated by `scripts/p5_drug_arm_feasibility.py` (seed 20260710). Design LOCKED by `manuscript/drug_timing_arm_scout.md` §3 (feasibility mode).
> **This is a plumbing/robustness check, not evidence for H_main.** It demonstrates the join + ortholog + nested-model + permutation machinery runs end-to-end on public data, and reports the decay-controlled INCREMENT of baseline chromatin features. A null/weak increment is an expected, acceptable feasibility outcome (§3.1–§3.2).

## 0. Verdict
- **Feasibility: PASS.** Pipeline runs end-to-end — public data acquired (GEO FTP + MGI) → MGI 1:1 mouse→human ortholog join → decay-controlled nested logistic → chromosome-blocked hold-out AUC + permutation null, all reproducible. ✅ This machinery is the deliverable.
- **Result: clean NULL.** Locked full model (decay + chromatin-lag + ATAC) vs decay-only base: ΔAUC = **-0.029**, LR p = **0.974**, permutation p (held-out ΔAUC) = **0.748** → **increment NOT significant (null)**. The cleanest 1-DOF increment (M1, decay+lag) is also null: ΔAUC -0.034, LR p 0.811, perm p 0.759. All McFadden R² ≈ 0.
- **Even the decay-only base carries no signal here** (CV AUC = 0.163, i.e. below 0.5 — no usable out-of-fold signal at n=72, not an inverse predictor). So there is no baseline for chromatin to add to. This is the **EXPECTED** outcome for a principled reason (§0.1), not broken plumbing.

### 0.1 Why a null is expected here — feature↔outcome timescale mismatch (primary limit)
The outcome is sampled at **5 d and 14 d** = **~10–20 mRNA half-lives** (Todorovski K562 t½ is single-digit hours, capped at 24 h). Every non-censored gene is fully mRNA-equilibrated long before day 5, so **decay t½ has essentially no mechanistic channel to predict a day5-vs-day14 ordinal** — the base model is asked to predict on a timescale where its native mechanism (Todorovski's 2 h/6 h decay→response) does not operate. Likewise the chromatin→transcription **lag** encodes minutes-to-hours coupling, while day-scale onset differences reflect cellular/differentiation dynamics. **Both base and increment are asked to predict a days-scale ordinal that neither feature's native timescale matches.** The honest reading of the null is therefore *'the design cannot test the hypothesis at this sampling cadence / in this coarse mouse proxy'*, **NOT** *'chromatin lag does not predict drug-response timing'* (a biological claim this data cannot make). The arm's tier is unchanged (no headline).

## 1. Data acquired (provenance)
| role | dataset | file | sha256 (head) |
|---|---|---|---|
| outcome | GSE201662 (mouse IDH1m AML, AG-120) | `data/GSE201662_Table_S2_AG120_DEGs.xlsx` | `c72f84e7fbe0a876` |
| outcome(raw) | GSE201662 | `data/GSE201662_raw_counts.txt.gz` | `bc5ebfdc87e2efd7` |
| decay t½ (control) | GSE229314 (Todorovski K562) | `data/todorovski_k562_halflife.csv` | `2e9fcba29f019282` |
| ortholog 1:1 | MGI HOM | `data/HOM_MouseHumanSequence.rpt` | `4823cbf8ea7961d1` |
| HSPC lag/α | this project | `results/multivelo_genes.csv` | `7564d500200e485e` |
| HSPC day0 ATAC | this project | `results/atac_baseline_features.csv` | `22330749ac01691f` |

GSE201662 URL: https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE201662 (GEO FTP suppl). GSE229314 t½ provenance: `data/PROVENANCE_halflife.md`. MGI HOM_MouseHumanSequence.rpt: https://www.informatics.jax.org/downloads/reports/ .

## 2. Outcome construction (§3.2 feasibility, mouse coarse)
- Author limma DEG (GSE201662 Table S2), AG-120 vs **matched Vehicle** at day5 and day14. **4-week relapse arm EXCLUDED** (regime change) — S2 contains only day5/day14 AG120 contrasts.
- **No per-gene t½ fit** (forbidden by §3.2 for this coarse 2-point mouse proxy).
- Responder = adj.P.Val < 0.05 at day5 **or** day14: 7134 mouse genes (day5 sig 6058, day14 sig 5016, late-only 1076).
- **Ordinal onset label:** `late_onset = 1` if response still RISING 5d→14d (|logFC_14|>|logFC_5|), else `0` (already peaked/plateaued by day5 = early onset). Chosen over a significance-onset label because day5 alone is already significant for most genes (→ degenerate 86/14 imbalance); the magnitude-trajectory label gives usable class balance and is the natural 2-point onset ordinal.

## 3. Intersection size (reported FIRST — §3.3 power caveat)
- MGI 1:1 high-confidence mouse→human orthologs: **18782**.
- Todorovski t½ genes: **6579**  ∩  HSPC lag∩ATAC genes: **511**  ∩  ortholog-mapped GSE201662 responders (with chrom): **join →**
- **FINAL analyzable intersection: n = 72 genes** (late_onset = 46, early_onset = 26; balance 0.64; 4 dropped for feature NaN — complete-case).
- ⚠️ **Small & triple-system-stacked** (HSPC lag ∩ K562 decay ∩ mouse-AML outcome). The binding constraint is the HSPC MultiVelo lag set (~538 genes). n=72 is **underpowered** for a small incremental effect regardless of true effect size — read the increment test as a plumbing demonstration, not a powered inference.

## 4. Nested models — decay-controlled increment (§3.1)
Outcome = `late_onset`. Base is decay-t½ only; every other model adds features OVER decay. `cv_auc` = leave-one-chromosome-out pooled-OOF AUC (chromosome-blocked hold-out, scaler fit on train fold only — no leakage). `LR_p` = in-sample likelihood-ratio χ² vs base. `perm_p` = one-sided permutation null on the held-out ΔAUC (B=2000).

| model | features | CV AUC | McFadden R² | ΔAUC vs base | LR p | perm p |
|---|---|---|---|---|---|---|
| M0_base_decay | log_thalf | 0.163 | 0.000 | 0.000 | NA | NA |
| M1_full_lag | log_thalf+lag | 0.129 | 0.001 | -0.034 | 0.811 | 0.759 |
| M2_full_locked | log_thalf+lag+prom_acc+enh_acc | 0.134 | 0.002 | -0.029 | 0.974 | 0.748 |
| M3_sec_atac | log_thalf+prom_acc+enh_acc | 0.160 | 0.002 | -0.003 | 0.923 | 0.627 |
| M4_sec_alpha | log_thalf+alpha_c | 0.078 | 0.000 | -0.085 | 0.951 | 0.973 |

- **Decay-only base CV AUC = 0.163** (below 0.5): decay t½ has no usable out-of-fold signal for this day-scale ordinal — primarily the timescale mismatch (§0.1), secondarily the IDH1i drug-class mismatch (§3.4 #4). All McFadden R² ≈ 0 (base 0.0000).
- **Headline locked increment = M1 (decay+lag, 1 added DOF — the most powered increment at n=72):** ΔAUC -0.034, LR p 0.811. The full locked-spec M2 (decay+lag+ATAC, 3 added DOF): ΔAUC -0.029, LR p 0.974, perm p 0.748 — M2 spends DOF n=72 cannot afford, so M1 carries interpretation. Both null.
- §6-consistent secondary routes (M3 ATAC-only, M4 α) reported alongside, also null. The LOCKED contribution per §3.1 is the chromatin-lag increment OVER decay (reported ONLY as an increment); it is not significant.

## 5. Confounds stated honestly (§3.4 — all six)
1. **Cell-context mismatch (max confound):** baseline = human HSPC; decay = human K562 leukemia line; outcome = mouse IDH1m AML. Gene-intrinsic-feature restriction (§3.3) mitigates but does not remove it. Only gene-intrinsic properties (lag, α, t½, promoter/enhancer accessibility as a stated-assumption covariate) cross systems — no absolute accessibility values transferred.
2. **Triple-system stacking:** three organisms/systems stitched by gene (HSPC + K562 + mouse AML) — more context confound than the 2-system pairing anticipated; the n=72 intersection is the price.
3. **Feature-intersection power:** n=72 (late 46/early 26). Underpowered for a small increment irrespective of effect size — the leading caveat.
4. **Drug-class heterogeneity:** AG-120 = IDH1i (metabolic→demethylation), NOT a chromatin writer / transcriptional inhibitor. No pooling with HMA/HDACi/BETi; base decay model may be off-mechanism.
5. **Pseudotime ≠ wall-clock + feature↔outcome timescale mismatch (the primary limit, §0.1):** HSPC lag is in pseudotime units and encodes minutes-to-hours chromatin→transcription coupling; decay t½ is single-digit hours; but the outcome onset is sampled at 5 d / 14 d (~10–20 half-lives). Neither feature's native timescale matches a day-scale ordinal, so the null is expected by construction — the design cannot test the hypothesis at this cadence. This is the honest headline caveat.
6. **Multiple testing / leakage:** chromosome-blocked hold-out (LOCO), scaler fit on train fold only, permutation null for the ΔAUC significance. Only the locked increment is the claim.

**Extra:** Todorovski K562 t½ has ~8% right-censoring at 24 h; used as log2 t½ (rank-robust), censor flag in the genes CSV (5 censored in the intersection).

## 6. Files
- `results/drug_arm_feasibility_genes.csv` — per-gene joined table (outcome + all features + chrom).
- `results/drug_arm_feasibility_models.csv` — nested-model comparison.
- `results/drug_arm_feasibility_permutation.csv` — held-out ΔAUC permutation null.

_Reproduce:_ `conda run -n scv-preprocess python scripts/p5_drug_arm_feasibility.py` (seed 20260710).
